[project]
name = "food-recs"
version = "0.1.0"
description = "Extract structured data from a YouTube channel into CSV"
requires-python = ">=3.12"
dependencies = [
    "anthropic>=0.117.0",
    "python-dotenv>=1.2.2",
    "rapidfuzz>=3.14.5",
    "requests>=2.34.2",
]
